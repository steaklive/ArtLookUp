<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ArtLookUp.App">
    <Application.Resources>

        <Color x:Key="AppBackgroundColor">#b6b6b6</Color>

        <Color x:Key="AndroidNavigationBarColor">#acaf82</Color>
        <Color x:Key="iOSNavigationBarColor">#acaf82</Color>

        <Color x:Key="iOSNavigationBarTextColor">White</Color>
        <Color x:Key="AndroidNavigationBarTextColor">White</Color>

        <!-- Implicit styles -->
        <Style TargetType="{x:Type NavigationPage}">
            <Setter Property="BarBackgroundColor"
                Value="{StaticResource AndroidNavigationBarColor}"
            />
                   
            <Setter Property="BarTextColor"
                    Value="{StaticResource AndroidNavigationBarTextColor}" 
            />
        
        </Style>

        <Style TargetType="{x:Type ContentPage}"
               ApplyToDerivedTypes="True">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
        
    </Application.Resources>
</Application>